# 错误日志监控与自动修复（routine `error-log-watch` 约定）

> 本文件为「新团队初始化包」快照副本（snapshot: 2026-09-13），泛化自来源仓库机制库（旧版本文档已清理，本文件为事实源）。
> 占位章节已于 **2026-09-13 OMB 团队初始化**时按 fork 仓库实况回填。OpenMausBot 为本地优先桌面应用，无统一应用日志文件：服务端日志走 stdout/stderr（dev 模式控制台），Electron 进程日志由桌面壳管理，会话级临时日志见 session info 的 logPath。**具体文件路径约定待第一个需求落地后按实际配置确认**。

| 项 | 约定（初始化后回填） |
|---|---|
| 日志路径 | `待确认（OpenMausBot 无统一应用日志文件；服务端 stderr / 会话临时日志，具体路径第一个需求落地后按实际配置回填）` |
| 错误来源 | 服务端 stderr（`node server/openmausbot.ts` 控制台输出）；前端 React SPA 浏览器 console（dev 模式）；Electron 进程日志（桌面壳管理） |
| 巡检周期 | 模板默认每 60 分钟（可调） |
| 错误分级 | 真缺陷/回归 → 标准 SDD 流程；环境/栈 → Build；琐碎 → 直连 Implement |
| 自动修复边界 | 修复必须走完整流程（规格→计划→对抗审查→人等批 Reviewed→TDD→QA），无人批准 Reviewed 不得改码/部署 |
