# 门禁只增加

写测试脚本或构建命令前，先读现有架构：语言、运行时、包管理器、测试框架、依赖版本。

完成标准：脚本能在**当前**工具链上跑通一条冒烟。沿用仓库里已有的版本号。

配置变更优先零侵入：能靠命令行参数 / 临时 init script / `--no-save` 拿到的报表，不改构建文件；必须写构建文件时，先出 diff 请用户批准。

用户豁免并记下责任人后可以先写业务代码。豁免记录是编码完成条，不是测试已通过。
