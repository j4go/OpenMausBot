# 继承说明

底本：Limix `develop-ui` 完整套件 `skills/product-workflow-suite/`。

增量（本会话）：

- 看板生成器：去 Playwright、本地 mermaid、`--restore prev`
- `product-wrap-up`
- 单测：问题 8 / 7b / 14 写入 `product-unit-test-init`（先认环境再出 `.ps1`/`.sh`，无 BOM，豁免可编码不得宣称完成）
